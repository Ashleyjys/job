# AI 工作流程执行脚本

## 使用目标
本脚本用于把 `test.md` 中的题目要求拆解为一套可直接交给 AI 执行的企业化实施流程。

执行原则：
1. 先输出系统整体框架，再进入逐步搭建。
2. 每一阶段都补齐对应规范文档。
3. 每一个实现步骤都尽量可验证、可追踪、可复用。
4. 优先使用最小可运行版本（MVP）完成主链路，再做增强。

---

## 第一阶段：项目整体框架梳理
让 AI 先输出以下内容，不要直接写代码：

### 1. 项目定位
- 业务场景名称
- 用户对象
- 业务目标
- MVP 范围
- 非目标范围

### 2. 系统总体架构
要求 AI 输出：
- 前端、后端、工作流、外部数据接口、算法接口之间的关系
- 数据主链路：数据获取 -> 数据清洗/标准化 -> 算法处理 -> 视图模型组装 -> 页面展示
- 模块分层：展示层、服务层、适配层、配置层、测试层

### 3. 文档清单
要求 AI 先规划再生成以下文档：
- 项目章程
- 业务范围说明
- 系统架构说明
- API 规范
- Figma / UI 复用说明
- 工作流设计文档
- 数据契约文档
- 测试计划
- AI Skills 使用记录
- 代码审查报告
- 部署与运行说明
- 最终交付清单

---

## 第二阶段：技术方案冻结
让 AI 在编码前明确以下内容：
- 前端：Vue 3 + TypeScript + Vite
- 后端：FastAPI
- 工作流配置：YAML
- 图表：ECharts
- 测试：pytest + Vitest
- 文档：Markdown

并要求 AI 输出：
- 技术选型理由
- 各模块职责边界
- 最小可运行链路的实现顺序

---

## 第三阶段：先搭骨架，再接主链路
要求 AI 严格按以下顺序执行：

### Step 1：搭建项目目录骨架
生成：
- `frontend/`
- `backend/`
- `docs/`
- `workflows/`
- `skills/`
- `.env.example`
- `README.md`

### Step 2：后端最小骨架
实现：
- FastAPI 应用入口
- `/health` 健康检查
- 工作流 YAML 加载器
- 看板查询接口 `/api/v1/dashboard/query`
- 统一响应模型

### Step 3：前端最小骨架
实现：
- Vite + Vue 3 + TypeScript
- 看板页面入口
- 城市选择组件
- 指标卡组件
- 风险面板组件
- 状态面板组件
- API 调用封装

### Step 4：主业务链路打通
实现：
- 城市输入
- 后端读取工作流
- 获取天气 / AQI 数据（先用 mock 也可以）
- 风险评分与异常检测
- 前端渲染返回结果

---

## 第四阶段：企业化规范补全
要求 AI 在实现过程中同步补齐：
- 接口文档
- 数据字段契约
- 错误处理规则
- 环境变量说明
- 测试计划与测试用例
- AI 生成内容记录
- 代码审查意见与修正记录

---

## 第五阶段：验证与交付
要求 AI 在宣告完成前必须执行：
1. 运行后端测试
2. 运行前端测试
3. 验证 `/health`
4. 验证 `/api/v1/dashboard/query`
5. 核对文档是否齐全
6. 核对文件编码是否为 UTF-8

---

## 对 AI 的执行约束
可以把下面这段直接作为提示词使用：

> 请基于 `test.md` 的要求实施项目，必须先输出系统整体框架，再按框架逐步搭建。整个过程采用企业项目交付方式推进，先文档化、再骨架、后实现、最后验证。每个阶段要同步生成规范文档，代码结构要分层、可复用、可测试。所有中文文档与代码文件统一使用 UTF-8 编码，不要使用会导致 PowerShell 管道丢失中文的写入方式。
