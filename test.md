# 试题：基于 AI 工具 Skills 的数据处理应用开发

## 背景
现代软件开发中，AI 辅助编程工具（如 Claude、 Copilot、Cursor、ChatGPT 等）的 Skills（技能/能力）可以帮助开发者快速完成重复性任务、整合外部资源、规范工作流程。本试题旨在考察你如何利用 AI 工具的 Skills，结合已有的网络算法接口、网络数据接口以及前端界面元素（Figma 设计），构建一个完整的云端数据处理应用。

## 任务目标
请你自主设计一个轻量级的数据处理 Web 应用，要求：

- 调用至少一个已有的网络算法接口（例如：图像识别、文本摘要、数据拟合、情感分析等公开 API，或你自己部署的算法服务）。
- 调用至少一个已有的网络数据接口（例如：RESTful 数据源、WebSocket 实时数据、公开数据集 API 等）。
- 复用已有的前端界面元素（基于 Figma 设计稿或公开 UI 组件库，需说明来源与适配方式）。
- 基于 AI 工具的 Skills 能力，完成以下工作：
  - 设计并实现一个可配置的工作流，将数据获取 → 算法处理 → 结果展示串联起来。
  - 确保代码具有可复用的架构（如模块化、分层设计、配置与逻辑分离等）。
  - 编写符合规范的代码，并通过代码审查（模拟或真实 review 流程）。

## 具体要求

### 1. 业务场景与功能
- 由你自行定义具体的数据处理领域（如舆情分析、实时数据监控、图像批处理等）。
- 应用应能完成：获取外部数据 → 调用算法处理 → 在前端界面上展示结果。
- 交互方式不限（Web 页面、仪表板、表单上传等），但必须使用 Figma 中已有的界面元素（可来自社区或自己设计的 Figma 文件）。

### 2. 技术约束
- **算法接口**：需是网络可调用的 HTTP/WebSocket 接口。你可以使用公开免费 API（如 OpenAI、百度AI、天行数据等），也可以自己用 Flask/FastAPI 等快速搭建一个模拟算法服务（需提供接口文档或代码）。
- **数据接口**：可以是第三方 API（如天气、股票、新闻、数据库查询接口等），或模拟的 REST API。
- **前端界面**：必须引用一个已有的 Figma 设计稿中的组件（按钮、卡片、图表、输入框等）。你需要通过代码实现该组件的样式与交互（可使用 HTML/CSS、React、Vue 等任意前端技术），并在文档中说明 Figma 的出处与如何复用设计。
- **工作流**：应支持用户配置或切换不同的数据源、算法参数（例如通过 UI 下拉菜单或配置文件）。工作流可以是顺序执行、条件分支或并行，需体现可编排性。
- **AI Skills 运用**：你需要使用至少一种 AI 编程工具（如 Copilot、ChatGPT、Codeium 等）来辅助完成以下至少两项：
  - 生成工作流编排代码
  - 生成 API 调用封装
  - 生成前端组件与 Figma 设计的匹配代码
  - 生成单元测试或代码重构建议
  - 进行代码审查（可让 AI 模拟审查并输出改进意见）

### 3. 代码质量与架构
- **可复用性**：代码应分层（如数据层、服务层、展示层），核心逻辑与配置解耦，方便扩展新的数据接口或算法。
- **可读性**：遵循命名规范、添加必要的注释、提供 README。
- **错误处理**：对网络请求失败、算法超时、数据格式错误等要有健壮处理。
- **代码审查**：你需要自行模拟代码审查（例如请 AI 工具或同学对你的代码进行审查），并提交审查记录和根据审查意见修改后的最终代码。

### 4. 交付物
你需要提交一个完整的项目文件夹（或代码仓库链接），**其中任务最终上交的项目文档必须包含 skills 文件**。交付物具体包含：

- **源代码**：前端 + 后端（如有）。
- **配置文件**：如 API 密钥示例（实际密钥不提交）、工作流定义文件（JSON/YAML）等。
- **项目文档（PDF 或 Markdown，且必须包含 skills 文件）**：
  - 应用功能说明与设计思路
  - 使用的算法接口、数据接口的详细说明（URL、请求/响应格式）
  - 引用的 Figma 设计链接或截图，并说明如何复用其中的元素
  - 工作流设计与编排逻辑（流程图或文字描述）
  - AI Skills 使用记录：包括你使用的 AI 工具、提示词（Prompt）、生成的关键代码片段、AI 帮助你解决的具体问题等（可附截图或文字日志）
  - 代码审查报告：谁（或 AI）做了审查？指出了哪些问题？你如何修改？
- **运行说明**：如何安装依赖、配置环境变量、启动应用。

---

> **补充说明：**
> - 你可以自由选择编程语言和框架（推荐前后端分离或全栈一体）。
> - 不必实现复杂的安全性、认证机制，但需体现基本的数据校验。
> - 如果算法接口或数据接口需要付费，请使用免费试用或模拟数据替代，但必须在文档中说明真实接口预期行为。

**祝你在本次测试中展示出你的自主学习能力、架构设计能力以及 AI 工具的驾驭水平！**